<h1>Add new task</h1>

<%= form_for @task do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :details %>
  <%= t.text_field :details %>
  <%= t.submit "add this task!" %>
<% end %>

<%= link_to 'Back to list', tasks_path%>
